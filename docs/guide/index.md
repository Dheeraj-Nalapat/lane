---
template: home.html
description: lane is a command-line tool that runs every git worktree or project as an isolated dev stack at its own *.localhost URL — no port conflicts, a shared proxy routes by hostname. Bring up just the services you changed and borrow the rest from a running base stack, with every service auto-routed at its own slug-service.localhost subdomain. Built for humans and AI coding agents to test in parallel and ship faster.
hide:
  - navigation
  - toc
---
